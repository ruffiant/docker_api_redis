# Logstash 主配置文件
input {
  tcp {
    mode => "server"
    host => "0.0.0.0"
    port => 5044
    codec => json_lines
  }
}
output {
  elasticsearch {
    hosts => ["http://localhost:9200"]
    user => "elastic"
    password => "DntKdS*4SG9ZMDFOBsh2"
    index => "%{[spring.application.name]}-%{+YYYY.MM.dd}"
  }
}
xpack {
  monitoring {
    enabled => true
  }
}